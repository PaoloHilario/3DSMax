macroScript catCaptureAnimation
            category:"CAT" 
            internalcategory:"CAT" 
            tooltip:"Open the Capture Animation Dialogue" 
            buttontext:"Capture Animation" 
            Icon:#("CAT",1)
(
   on execute do rCaptureAnim.openDialog();
 --  on isChecked 
)
