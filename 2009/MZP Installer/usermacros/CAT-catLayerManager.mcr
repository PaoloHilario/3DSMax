macroScript catLayerManager
            category:"CAT" 
            internalcategory:"CAT" 
            tooltip:"Layer Manager" 
            buttontext:"Layer Manager" 
            Icon:#("CAT",1)
(
   on execute do CreateCATLayerManager()
)
